{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "FastAPI App - Debug",
            "type": "shell",
            "command": "uvicorn app.main:app --reload",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}